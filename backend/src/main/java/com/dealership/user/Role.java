package com.dealership.user;

/**
 * User roles in the system.
 * USER  — can browse and purchase vehicles.
 * ADMIN — can also add, update, delete vehicles and restock inventory.
 */
public enum Role {
    USER,
    ADMIN
}
